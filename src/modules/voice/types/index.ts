/**
 * Type definitions for kiosk-voice-capture feature
 */

// Add your feature-specific types here
export type KioskVoiceCaptureConfig = Record<string, never>

export type KioskVoiceCaptureState = Record<string, never>
